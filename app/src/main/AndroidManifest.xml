<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.nigtime456.weatherapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/BaseTheme.Dark">

        <activity android:name="com.gmail.nigtime456.weatherapplication.ui.screen.current.pager.CurrentForecastActivity" />

        <activity android:name="com.gmail.nigtime456.weatherapplication.ui.screen.search.SearchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.gmail.nigtime456.weatherapplication.ui.screen.locations.LocationsActivity"
            android:parentActivityName="com.gmail.nigtime456.weatherapplication.ui.screen.current.pager.CurrentForecastActivity" />

        <activity
            android:name="com.gmail.nigtime456.weatherapplication.ui.screen.splash.SplashActivity"
            android:theme="@style/SplashTheme">

        </activity>

    </application>

</manifest>